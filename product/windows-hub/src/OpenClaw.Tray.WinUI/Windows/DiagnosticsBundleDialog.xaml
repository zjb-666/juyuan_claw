<?xml version="1.0" encoding="utf-8"?>
<ContentDialog x:Uid="DiagnosticsBundleDialog_DiagnosticsBundle"
    x:Class="OpenClawTray.Windows.DiagnosticsBundleDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Style="{StaticResource DefaultContentDialogStyle}"
    Title="Diagnostics bundle"
    PrimaryButtonText="Copy to clipboard"
    SecondaryButtonText="Save to file"
    CloseButtonText="Close"
    DefaultButton="Primary">

    <Grid RowSpacing="12" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            HorizontalAlignment="Stretch"
            Padding="14"
            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="4">
            <Grid ColumnSpacing="12" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <FontIcon
                    Grid.Column="0"
                    Glyph="&#xE946;"
                    FontSize="16"
                    Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                    FontFamily="{ThemeResource SymbolThemeFontFamily}"/>
                <StackPanel Grid.Column="1" Spacing="6" HorizontalAlignment="Stretch">
                    <TextBlock
                        x:Uid="DiagnosticsBundleDialog_ReviewBeforeSharingTitle"
                        Text="Review before sharing"
                        FontWeight="SemiBold"
                        HorizontalAlignment="Stretch"
                        TextWrapping="WrapWholeWords"/>
                    <TextBlock
                        x:Uid="DiagnosticsBundleDialog_ReviewBeforeSharingMessage"
                        Text="Log tails are sanitized and truncated. Sensitive tokens, payloads, recordings, and raw settings files are excluded. Review before sharing."
                        HorizontalAlignment="Stretch"
                        TextWrapping="WrapWholeWords"/>
                </StackPanel>
            </Grid>
        </Border>

        <TextBlock
            Grid.Row="1"
            x:Name="BundleHeaderText"
            Style="{StaticResource CaptionTextBlockStyle}"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            TextWrapping="Wrap"/>

        <Border
            Grid.Row="2"
            HorizontalAlignment="Stretch"
            Background="{ThemeResource ControlAltFillColorTertiaryBrush}"
            BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="4">
            <ScrollViewer
                Height="420"
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Auto">
                <TextBox
                    x:Name="BundleText"
                    IsReadOnly="True"
                    AcceptsReturn="True"
                    TextWrapping="NoWrap"
                    FontFamily="Consolas"
                    FontSize="12"
                    HorizontalAlignment="Stretch"
                    BorderThickness="0"
                    Background="Transparent"
                    AutomationProperties.Name="Diagnostics bundle text"/>
            </ScrollViewer>
        </Border>
    </Grid>
</ContentDialog>
