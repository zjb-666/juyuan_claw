<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="OpenClawTray.Windows.CommandPaletteDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Style="{StaticResource DefaultContentDialogStyle}"
    FullSizeDesired="False"
    CloseButtonText=""
    Opened="OnOpened">

    <StackPanel Width="500" Spacing="8">
        <AutoSuggestBox x:Uid="SearchBox" x:Name="SearchBox"
                        PlaceholderText="Type a command..."
                        QueryIcon="Find"
                        TextChanged="OnSearchTextChanged"
                        QuerySubmitted="OnQuerySubmitted"/>
        <ListView x:Name="ResultsList"
                  MaxHeight="400"
                  SelectionMode="Single"
                  IsItemClickEnabled="True"
                  ItemClick="OnResultClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="12" Padding="8,6">
                        <TextBlock Text="{Binding Icon}" FontSize="16" VerticalAlignment="Center"/>
                        <StackPanel Spacing="2">
                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Subtitle}" FontSize="11"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</ContentDialog>
