<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClawTray.Pages.SessionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Enabled">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid HorizontalAlignment="Stretch">
            <StackPanel Padding="24" Spacing="12"
                    HorizontalAlignment="Stretch" MaxWidth="900">

            <TextBlock x:Uid="SessionsPage_Sessions"
                           Text="Sessions"
                           AutomationProperties.AutomationId="SessionsPageMarker"
                           Style="{StaticResource TitleTextBlockStyle}"
                           Margin="0,0,0,2"/>
                <TextBlock x:Uid="SessionsPage_Subtitle" Text="Conversations this gateway is currently handling, grouped by channel."
                           Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           TextWrapping="Wrap" Margin="0,0,0,4"/>

                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <SelectorBar x:Name="ChannelSelector" Grid.Column="0"
                                 VerticalAlignment="Center"
                                 SelectionChanged="ChannelSelector_SelectionChanged">
                        <SelectorBarItem x:Uid="SessionsPage_All" x:Name="AllTab" Text="All" IsSelected="True"/>
                    </SelectorBar>

                    <ToggleSwitch x:Uid="SessionsPage_ShowCompleted" x:Name="ShowCompletedToggle" Grid.Column="1"
                                  Header="Show ended"
                                  MinWidth="0"
                                  OnContent="" OffContent=""
                                  Toggled="OnShowCompletedToggled"
                                  AutomationProperties.AutomationId="SessionsPageShowCompleted"/>

                    <ToggleSwitch x:Uid="SessionsPage_ShowBackground" x:Name="ShowBackgroundToggle" Grid.Column="2"
                                  Header="Show background"
                                  MinWidth="0"
                                  OnContent="" OffContent=""
                                  Toggled="OnShowBackgroundToggled"
                                  AutomationProperties.AutomationId="SessionsPageShowBackground"/>

                    <Button x:Name="RefreshButton" Grid.Column="3"
                            Click="OnRefresh"
                            ToolTipService.ToolTip="Refresh sessions"
                            AutomationProperties.Name="Refresh sessions">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon x:Name="RefreshIcon"
                                      Glyph="&#xE72C;"
                                      FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                      FontSize="14"
                                      IsTextScaleFactorEnabled="False"/>
                            <TextBlock x:Uid="SessionsPage_Refresh" x:Name="RefreshLabel" Text="Refresh"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <InfoBar x:Uid="SessionsPage_GatewayDisconnected"
                         x:Name="ConnectionInfoBar"
                         IsOpen="False" IsClosable="False" Severity="Warning"
                         Title="Gateway disconnected"
                         Message="Connect to a gateway to load sessions.">
                    <InfoBar.ActionButton>
                        <Button x:Uid="SessionsPage_OpenConnection" Content="Open Connection" Click="OnOpenConnectionClick"/>
                    </InfoBar.ActionButton>
                </InfoBar>

                <!-- Loading / empty / list share this slot; only one is Visible at a time. -->
                <Grid MinHeight="160">
                    <StackPanel x:Name="LoadingState"
                                Orientation="Horizontal" Spacing="12"
                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                Visibility="Collapsed">
                        <ProgressRing IsActive="True" Width="20" Height="20"/>
                        <TextBlock x:Uid="SessionsPage_LoadingSessions"
                                   Text="Loading sessions..."
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel x:Name="EmptyState" Spacing="8"
                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                Visibility="Collapsed">
                        <FontIcon Glyph="&#xE8BD;"
                                  FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                  FontSize="40"
                                  IsTextScaleFactorEnabled="False"
                                  HorizontalAlignment="Center"
                                  Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                        <TextBlock x:Uid="SessionsPage_NoActiveSessions"
                                   Text="No active sessions"
                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock x:Uid="SessionsPage_EmptyHint" Text="Sessions appear here when a channel is connected and traffic is flowing."
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   TextWrapping="Wrap" TextAlignment="Center"
                                   MaxWidth="320"/>
                    </StackPanel>

                    <ListView x:Name="SessionListView"
                              SelectionMode="None"
                              ShowsScrollingPlaceholders="False">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="MinHeight" Value="0"/>
                                <Setter Property="Margin" Value="0,0,0,8"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1" CornerRadius="8"
                                        Padding="16,12">
                                    <Grid RowSpacing="8" ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Row="0" Grid.Column="0"
                                                    Orientation="Horizontal" Spacing="10"
                                                    VerticalAlignment="Center">
                                            <Ellipse Width="10" Height="10"
                                                     VerticalAlignment="Center"
                                                     Fill="{Binding StatusBrush}"
                                                     ToolTipService.ToolTip="{Binding StatusTooltip}"/>
                                            <TextBlock Text="{Binding DisplayName}"
                                                       Style="{StaticResource BodyStrongTextBlockStyle}"
                                                       TextTrimming="CharacterEllipsis"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>

                                        <StackPanel Grid.Row="0" Grid.Column="1"
                                                    Orientation="Horizontal" Spacing="8"
                                                    VerticalAlignment="Center">
                                            <TextBlock Text="{Binding AgeText}"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>

                                        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                                   Text="{Binding DetailLine}"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                   TextTrimming="CharacterEllipsis"
                                                   Margin="20,0,0,0"/>

                                        <Grid Grid.Row="2" Grid.Column="0"
                                              Margin="20,0,8,0"
                                              ColumnSpacing="12"
                                              VerticalAlignment="Center"
                                              Visibility="{Binding TokenRowVisibility}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <ProgressBar Value="{Binding ContextPercent}" Maximum="100"
                                                         Height="4" VerticalAlignment="Center"
                                                         CornerRadius="2"/>
                                            <TextBlock Grid.Column="1" Text="{Binding TokensText}"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                        </Grid>

                                        <StackPanel Grid.Row="2" Grid.Column="1"
                                                    Orientation="Horizontal" Spacing="4"
                                                    VerticalAlignment="Center">
                                            <Button Tag="{Binding Key}"
                                                    Click="OnOpenChat"
                                                    Style="{StaticResource AccentButtonStyle}"
                                                    x:Uid="SessionsPage_OpenChatButton" Content="Open chat"
                                                    ToolTipService.ToolTip="Open this session in Chat"
                                                    AutomationProperties.Name="Open in chat"/>
                                            <Button Tag="{Binding Key}"
                                                    IsEnabled="{Binding CanEdit}"
                                                    ToolTipService.ToolTip="More actions"
                                                    AutomationProperties.Name="More actions">
                                                <FontIcon Glyph="&#xE712;"
                                                          FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                          FontSize="14"
                                                          IsTextScaleFactorEnabled="False"/>
                                                <Button.Flyout>
                                                    <MenuFlyout Placement="BottomEdgeAlignedRight">
                                                        <MenuFlyoutItem x:Uid="SessionsPage_Reset"
                                                                        Text="Reset"
                                                                        Tag="{Binding Key}"
                                                                        Click="OnResetSession">
                                                            <MenuFlyoutItem.Icon>
                                                                <FontIcon Glyph="&#xE72C;"
                                                                          FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                                          IsTextScaleFactorEnabled="False"/>
                                                            </MenuFlyoutItem.Icon>
                                                        </MenuFlyoutItem>
                                                        <MenuFlyoutItem x:Uid="SessionsPage_Compact"
                                                                        Text="Compact"
                                                                        Tag="{Binding Key}"
                                                                        Click="OnCompactSession">
                                                            <MenuFlyoutItem.Icon>
                                                                <FontIcon Glyph="&#xE74C;"
                                                                          FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                                          IsTextScaleFactorEnabled="False"/>
                                                            </MenuFlyoutItem.Icon>
                                                        </MenuFlyoutItem>
                                                        <MenuFlyoutItem x:Uid="SessionsPage_Checkpoints"
                                                                        Text="Checkpoints"
                                                                        Tag="{Binding Key}"
                                                                        Click="OnShowCheckpoints"
                                                                        ToolTipService.ToolTip="View, branch or restore compaction checkpoints">
                                                            <MenuFlyoutItem.Icon>
                                                                <FontIcon Glyph="&#xE81C;"
                                                                          FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                                          IsTextScaleFactorEnabled="False"/>
                                                            </MenuFlyoutItem.Icon>
                                                        </MenuFlyoutItem>
                                                        <MenuFlyoutItem x:Uid="SessionsPage_ExportTranscript"
                                                                        Text="Export transcript"
                                                                        Tag="{Binding Key}"
                                                                        Click="OnExportSession"
                                                                        ToolTipService.ToolTip="Save the conversation transcript to a text file">
                                                            <MenuFlyoutItem.Icon>
                                                                <FontIcon Glyph="&#xEDE1;"
                                                                          FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                                          IsTextScaleFactorEnabled="False"/>
                                                            </MenuFlyoutItem.Icon>
                                                        </MenuFlyoutItem>
                                                        <MenuFlyoutSeparator/>
                                                        <MenuFlyoutItem x:Uid="SessionsPage_Delete"
                                                                        Text="Delete"
                                                                        Tag="{Binding Key}"
                                                                        Click="OnDeleteSession"
                                                                        IsEnabled="{Binding CanDelete}"
                                                                        Foreground="{ThemeResource SystemFillColorCriticalBrush}">
                                                            <MenuFlyoutItem.Icon>
                                                                <FontIcon Glyph="&#xE74D;"
                                                                          FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                                          IsTextScaleFactorEnabled="False"
                                                                          Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                                                            </MenuFlyoutItem.Icon>
                                                        </MenuFlyoutItem>
                                                    </MenuFlyout>
                                                </Button.Flyout>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
