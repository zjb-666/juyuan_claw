<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClaw.SetupEngine.UI.Pages.CompletePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Disabled">

    <Grid RowDefinitions="*,Auto" Padding="48,28,48,24">

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel VerticalAlignment="Center" Spacing="12" HorizontalAlignment="Stretch"
                        MaxWidth="480" Margin="0,8">

                <Grid x:Name="SuccessIcon" Width="100" Height="100"
                      HorizontalAlignment="Center" Margin="0,0,0,4">
                    <Image Source="ms-appx:///OpenClaw.SetupEngine.UI/Assets/Setup/OpenClawMascot.png"
                           Width="100" Height="100"
                           AutomationProperties.AccessibilityView="Raw" />
                    <Border Width="36" Height="36" CornerRadius="18"
                            HorizontalAlignment="Right" VerticalAlignment="Bottom"
                            Background="{ThemeResource SystemFillColorSuccessBrush}"
                            BorderBrush="{ThemeResource SolidBackgroundFillColorBaseBrush}" BorderThickness="3">
                        <FontIcon Glyph="&#xE73E;" FontSize="16" IsTextScaleFactorEnabled="False"
                                  Foreground="#FFFFFF"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                  AutomationProperties.AccessibilityView="Raw" />
                    </Border>
                </Grid>
                <Grid x:Name="FailureIcon" Width="100" Height="100"
                      HorizontalAlignment="Center" Margin="0,0,0,16"
                      Visibility="Collapsed">
                    <Image Source="ms-appx:///OpenClaw.SetupEngine.UI/Assets/Setup/OpenClawMascot.png"
                           Width="100" Height="100"
                           AutomationProperties.AccessibilityView="Raw" />
                    <Border Width="36" Height="36" CornerRadius="18"
                            HorizontalAlignment="Right" VerticalAlignment="Bottom"
                            Background="{ThemeResource SystemFillColorCriticalBrush}"
                            BorderBrush="{ThemeResource SolidBackgroundFillColorBaseBrush}" BorderThickness="3">
                        <FontIcon Glyph="&#xEA39;" FontSize="16" IsTextScaleFactorEnabled="False"
                                 Foreground="#FFFFFF"
                                 HorizontalAlignment="Center" VerticalAlignment="Center"
                                 AutomationProperties.AccessibilityView="Raw" />
                    </Border>
                </Grid>

                <TextBlock x:Name="TitleText" Text="All set!"
                           Style="{StaticResource TitleTextBlockStyle}"
                           HorizontalAlignment="Center" />

                <TextBlock x:Name="SubtitleText" Text="聚元灵创已就绪 to go"
                           Style="{StaticResource BodyTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           HorizontalAlignment="Center" />

                <StackPanel x:Name="SummaryPanel" Spacing="8" Visibility="Collapsed">
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" Padding="14,12">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="14">
                            <FontIcon Grid.Column="0" Glyph="&#xE756;" FontSize="18" VerticalAlignment="Center" />
                            <StackPanel Grid.Column="1" Spacing="2">
                                <TextBlock Text="Local gateway running" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                <TextBlock x:Name="GatewaySummaryText" Text="聚元灵创网关 · 127.0.0.1:18789" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                            <FontIcon Grid.Column="2" Glyph="&#xE73E;" FontSize="16" Foreground="{ThemeResource SystemFillColorSuccessBrush}" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" Padding="14,12">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="14">
                            <FontIcon Grid.Column="0" Glyph="&#xE772;" FontSize="18" VerticalAlignment="Center" />
                            <StackPanel Grid.Column="1" Spacing="2">
                                <TextBlock Text="Device paired" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                <TextBlock Text="This PC is connected and ready to use" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                            <FontIcon Grid.Column="2" Glyph="&#xE73E;" FontSize="16" Foreground="{ThemeResource SystemFillColorSuccessBrush}" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" Padding="14,12">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="14">
                            <FontIcon Grid.Column="0" Glyph="&#xEA18;" FontSize="18" VerticalAlignment="Center" />
                            <StackPanel Grid.Column="1" Spacing="2">
                                <TextBlock Text="Capability profile applied" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                <TextBlock Text="Editable any time in Companion Settings" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                            <FontIcon Grid.Column="2" Glyph="&#xE73E;" FontSize="16" Foreground="{ThemeResource SystemFillColorSuccessBrush}" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </StackPanel>

                <Border x:Name="ErrorCard" CornerRadius="4" Padding="16,12,16,12"
                        Margin="0,16,0,0" Visibility="Collapsed"
                        Background="{ThemeResource SystemFillColorCriticalBackgroundBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel Spacing="8">
                        <TextBlock x:Name="ErrorText" TextWrapping="Wrap"
                                   FontSize="13" FontFamily="Cascadia Code,Consolas,monospace"
                                   Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
                        <HyperlinkButton x:Name="HelpLink"
                                         Visibility="Collapsed"
                                         Padding="0"
                                         FontSize="13" />
                        <HyperlinkButton x:Name="ViewLogLink" Content="View full log →"
                                         Click="ViewLog_Click" Padding="0"
                                         FontSize="13" />
                    </StackPanel>
                </Border>

                <InfoBar x:Name="NodeModeBanner"
                         IsOpen="True" IsClosable="False" Severity="Informational"
                         Title="Node mode enabled"
                         Message="This PC will operate as a remote compute node. The gateway can invoke screen capture, camera, and system commands on this machine." />

                <Border x:Name="StartupRow"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1" CornerRadius="4" Padding="14,6,8,6">
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBlock Grid.Column="0" Text="开机启动聚元灵创"
                                   VerticalAlignment="Center"
                                   Style="{StaticResource BodyTextBlockStyle}" />
                        <ToggleSwitch x:Name="StartupToggle" Grid.Column="1"
                                      OnContent="On" OffContent="Off"
                                      HorizontalAlignment="Right" VerticalAlignment="Center"
                                      MinWidth="0" />
                    </Grid>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="1" ColumnDefinitions="*,Auto" Margin="0,16,0,0">
            <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center" VerticalAlignment="Center"
                        IsTabStop="False" AutomationProperties.Name="Step 6 of 6">
                <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
                <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
                <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
                <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
                <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
                <Border Width="20" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupIndicatorAccentBrush}" />
            </StackPanel>
            <Button x:Name="LaunchButton" Grid.Column="1"
                    Content="Finish"
                    HorizontalAlignment="Right" MinWidth="100"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="LaunchButton_Click" />
        </Grid>

    </Grid>
</Page>
