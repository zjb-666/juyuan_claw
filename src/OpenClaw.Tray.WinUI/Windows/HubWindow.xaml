<?xml version="1.0" encoding="utf-8"?>
<winex:WindowEx x:Uid="HubWindow_winexWindowEx_2"
    x:Class="OpenClawTray.Windows.HubWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:OpenClawTray.Controls"
    xmlns:winex="using:WinUIEx"
    Title="聚元灵创"
    MinWidth="900" MinHeight="560"
    IsMaximizable="True"
    IsResizable="True"
    IsMinimizable="True">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" PreviewKeyDown="OnRootPreviewKeyDown">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Padding="9,0,140,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Lobster logo doubles as the nav pane toggle; hovering reveals the panel-left glyph (GitHub Copilot style) -->
            <Button x:Uid="NavPaneToggleButton" x:Name="NavPaneToggleButton"
                    Grid.Column="0"
                    AutomationProperties.Name="Toggle navigation pane"
                    MinWidth="32" MinHeight="32"
                    Padding="0" VerticalAlignment="Center"
                    CornerRadius="6"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    BorderThickness="0"
                    ToolTipService.ToolTip="Toggle navigation pane"
                    PointerEntered="OnBrandTogglePointerEntered"
                    PointerExited="OnBrandTogglePointerExited"
                    Click="OnNavPaneToggleButtonClick">
                <Button.Resources>
                    <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="{ThemeResource SubtleFillColorSecondary}"/>
                    <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="{ThemeResource SubtleFillColorTertiary}"/>
                </Button.Resources>
                <Grid Width="18" Height="18">
                    <controls:BrandMark x:Name="BrandToggleMark"
                                        MarkSize="18"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"/>
                    <FontIcon x:Name="BrandToggleGlyph"
                              Glyph="&#xE90C;"
                              FontFamily="{ThemeResource SymbolThemeFontFamily}"
                              FontSize="16"
                              Opacity="0"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Center"/>
                </Grid>
            </Button>

            <TextBlock x:Uid="TitleText" Grid.Column="1" x:Name="TitleText" Text="聚元灵创"
                        FontSize="13" VerticalAlignment="Center"
                        Margin="8,0,0,0"
                        Style="{StaticResource CaptionTextBlockStyle}"/>

            <!-- Back button + search travel together, centered, with the back chevron directly left of the search box (Teams style) -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="6"
                        HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button x:Uid="NavBackButton" x:Name="NavBackButton"
                        AutomationProperties.Name="Back"
                        MinWidth="32" MinHeight="32"
                        Padding="0" VerticalAlignment="Center"
                        CornerRadius="6"
                        IsEnabled="False"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        ToolTipService.ToolTip="Back"
                        Click="OnBackRequested">
                    <Button.Resources>
                        <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="{ThemeResource SubtleFillColorSecondary}"/>
                        <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="{ThemeResource SubtleFillColorTertiary}"/>
                    </Button.Resources>
                    <FontIcon Glyph="&#xE76B;"
                              FontFamily="{ThemeResource SymbolThemeFontFamily}"
                              FontSize="16"/>
                </Button>

                <!-- Search box -->
                <AutoSuggestBox x:Uid="TitleSearchBox" x:Name="TitleSearchBox"
                                 PlaceholderText="Search… (Ctrl+E)"
                                 QueryIcon="Find"
                                TextMemberPath="Title"
                                Width="420"
                                Height="32" VerticalAlignment="Center"
                                TextChanged="OnSearchTextChanged"
                                SuggestionChosen="OnSearchSuggestionChosen"
                                QuerySubmitted="OnSearchQuerySubmitted">
                    <AutoSuggestBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Spacing="8" Padding="4,4">
                                <TextBlock Text="{Binding Icon}" FontSize="14" VerticalAlignment="Center"/>
                                <StackPanel Spacing="0">
                                    <TextBlock Text="{Binding Title}" FontSize="12"/>
                                    <TextBlock Text="{Binding Subtitle}" FontSize="10"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </AutoSuggestBox.ItemTemplate>
                </AutoSuggestBox>
            </StackPanel>

            <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">

                <Border x:Name="ComputeBalancePill"
                        Height="30"
                        Padding="11,0"
                        CornerRadius="15"
                        Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                        VerticalAlignment="Center"
                        Visibility="Collapsed"
                        ToolTipService.ToolTip="平台算力余额（与网页钱包同源）">
                    <TextBlock x:Name="ComputeBalanceText"
                               Text="算力 —"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               VerticalAlignment="Center"/>
                </Border>

                <Button x:Name="StatusPillButton"
                        Height="30"
                        Padding="11,0"
                        CornerRadius="15"
                        Background="Transparent"
                        VerticalAlignment="Center"
                        AutomationProperties.Name="Connection status">
                    <Button.Resources>
                        <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="{ThemeResource SubtleFillColorSecondary}"/>
                        <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="{ThemeResource SubtleFillColorTertiary}"/>
                    </Button.Resources>
                    <StackPanel Orientation="Horizontal" Spacing="7" VerticalAlignment="Center">
                        <Ellipse x:Name="StatusPillDot" Width="10" Height="10" VerticalAlignment="Center" Fill="Gray"/>
                        <TextBlock x:Name="StatusPillText" Text="Disconnected"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   VerticalAlignment="Center"/>
                        <FontIcon Glyph="&#xE70D;" FontSize="8" VerticalAlignment="Center"
                                  Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                  FontFamily="{ThemeResource SymbolThemeFontFamily}"/>
                    </StackPanel>
                    <Button.Flyout>
                        <Flyout x:Name="StatusFlyout" Placement="Bottom" Opening="OnStatusFlyoutOpening">
                            <StackPanel Width="304" Spacing="0">
                                <Grid Padding="0,0,0,10" ColumnSpacing="11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Ellipse x:Name="GatewayRowDot" Width="10" Height="10" VerticalAlignment="Center" Fill="Gray"/>
                                    <StackPanel Grid.Column="1" Spacing="1">
                                        <TextBlock x:Uid="HubWindow_StatusFlyout_GatewayLabel" Text="Gateway"
                                                   Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <TextBlock x:Name="GatewayRowDetail" Text=""
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    <HyperlinkButton x:Name="GatewayRowAction" x:Uid="HubWindow_StatusFlyout_Reconnect"
                                                     Grid.Column="2" Content="Reconnect"
                                                     Padding="4,0" VerticalAlignment="Center"
                                                     Visibility="Collapsed"
                                                     Click="OnStatusFlyoutReconnectClick"/>
                                </Grid>
                                <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}"/>

                                <Grid Padding="0,10" ColumnSpacing="11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Ellipse x:Name="OperatorRowDot" Width="10" Height="10" VerticalAlignment="Center" Fill="Gray"/>
                                    <StackPanel Grid.Column="1" Spacing="1">
                                        <TextBlock x:Uid="HubWindow_StatusFlyout_OperatorLabel" Text="Operator"
                                                   Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <TextBlock x:Name="OperatorRowDetail" Text=""
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                </Grid>
                                <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}"/>

                                <Grid Padding="0,10" ColumnSpacing="11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Ellipse x:Name="NodeRowDot" Width="10" Height="10" VerticalAlignment="Center" Fill="Gray"/>
                                    <StackPanel Grid.Column="1" Spacing="1">
                                        <TextBlock x:Uid="HubWindow_StatusFlyout_NodeLabel" Text="Node"
                                                   Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <TextBlock x:Name="NodeRowDetail" Text=""
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                    <HyperlinkButton x:Name="NodeRowAction" x:Uid="HubWindow_StatusFlyout_Approve"
                                                     Grid.Column="2" Content="Approve…"
                                                     Padding="4,0" VerticalAlignment="Center"
                                                     Visibility="Collapsed"
                                                     Click="OnStatusFlyoutNodeActionClick"/>
                                </Grid>

                                <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}"/>
                                <HyperlinkButton x:Uid="HubWindow_StatusFlyout_OpenConnection"
                                                 Content="Open Connection"
                                                 Margin="2,8,2,0" Padding="4,0"
                                                 Click="OnStatusFlyoutOpenConnectionClick"/>
                            </StackPanel>
                        </Flyout>
                    </Button.Flyout>
                </Button>

                <Button x:Name="NotificationsBellButton"
                        MinWidth="36" Height="30"
                        Padding="0"
                        Background="Transparent"
                        BorderThickness="0"
                        BorderBrush="Transparent"
                        VerticalAlignment="Center"
                        AutomationProperties.Name="Notifications">
                    <Button.Resources>
                        <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="{ThemeResource SubtleFillColorSecondary}"/>
                        <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="{ThemeResource SubtleFillColorTertiary}"/>
                    </Button.Resources>
                    <Grid>
                        <FontIcon Glyph="&#xE7E7;" FontSize="16"
                                  FontFamily="{ThemeResource SymbolThemeFontFamily}"/>
                        <InfoBadge x:Name="NotificationsBadge"
                                   Value="0"
                                   Visibility="Collapsed"
                                   HorizontalAlignment="Right" VerticalAlignment="Top"
                                   Margin="0,-6,-8,0"/>
                    </Grid>
                    <Button.Flyout>
                        <Flyout x:Name="NotificationsFlyout" Placement="Bottom" Opening="OnNotificationsFlyoutOpening">
                            <Grid Width="352" MaxHeight="440" RowSpacing="0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0" Margin="2,0,2,8">
                                    <HyperlinkButton x:Name="BellClearAllButton" x:Uid="NotificationsFlyout_ClearAll"
                                                     Content="Clear all"
                                                     HorizontalAlignment="Right"
                                                     Padding="4,0" VerticalAlignment="Center"
                                                     Click="OnBellClearAllClick"/>
                                </Grid>

                                <ListView x:Name="BellNotificationsList"
                                          Grid.Row="1"
                                          SelectionMode="None"
                                          IsItemClickEnabled="False"
                                          Padding="0"
                                          Margin="-4,0">
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="Margin" Value="0"/>
                                            <Setter Property="MinHeight" Value="0"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="8"
                                                    Padding="12"
                                                    Margin="4,4,4,4">
                                                <Grid ColumnSpacing="11">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <FontIcon Grid.Column="0"
                                                              Glyph="{Binding SeverityGlyph}"
                                                              FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                              FontSize="16"
                                                              VerticalAlignment="Top"
                                                              Margin="0,2,0,0"/>
                                                    <StackPanel Grid.Column="1" Spacing="3">
                                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                                            <TextBlock Text="{Binding Title}"
                                                                       Style="{StaticResource BodyStrongTextBlockStyle}"
                                                                       TextWrapping="WrapWholeWords"/>
                                                            <Border Visibility="{Binding OccurrenceVisibility}"
                                                                    Background="{ThemeResource SystemFillColorCautionBackgroundBrush}"
                                                                    CornerRadius="10"
                                                                    Padding="7,1"
                                                                    VerticalAlignment="Center">
                                                                <TextBlock Text="{Binding OccurrenceText}"
                                                                           Style="{StaticResource CaptionTextBlockStyle}"/>
                                                            </Border>
                                                        </StackPanel>
                                                        <TextBlock Text="{Binding Message}"
                                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                                   TextWrapping="WrapWholeWords"/>
                                                        <TextBlock Text="{Binding Metadata}"
                                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                    </StackPanel>
                                                    <Button Grid.Column="2"
                                                            Tag="{Binding Id}"
                                                            AutomationProperties.Name="{Binding DismissAutomationName}"
                                                            Padding="6"
                                                            MinWidth="30" MinHeight="30"
                                                            VerticalAlignment="Top"
                                                            Background="Transparent"
                                                            BorderThickness="0"
                                                            Click="OnBellDismissNotificationClick">
                                                        <FontIcon Glyph="&#xE711;"
                                                                  FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                                  FontSize="11"/>
                                                    </Button>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>

                                <Border x:Name="BellEmptyState"
                                        Grid.Row="1"
                                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="20"
                                        Margin="0,4"
                                        VerticalAlignment="Top">
                                    <StackPanel Spacing="6">
                                        <TextBlock x:Uid="NotificationsFlyout_EmptyTitle"
                                                   Text="No notifications"
                                                   Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <TextBlock x:Uid="NotificationsPage_EmptyDescription"
                                                   Text="New app notifications will appear here until you dismiss or clear them."
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                   TextWrapping="WrapWholeWords"/>
                                    </StackPanel>
                                </Border>

                                <Grid Grid.Row="2" Margin="2,8,2,0" ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <HyperlinkButton x:Uid="NotificationsFlyout_OpenPage"
                                                     Content="Open notifications"
                                                     Padding="4,0" VerticalAlignment="Center"
                                                     Click="OnBellOpenPageClick"/>
                                    <TextBlock x:Name="BellActiveCountText" Grid.Column="1" Text=""
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </Grid>
                        </Flyout>
                    </Button.Flyout>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Main Content -->
        <Grid x:Name="NavContentHost"
              Grid.Row="1">

            <NavigationView
                x:Uid="HubWindow_MainNavigation"
                x:Name="NavView"
                IsBackButtonVisible="Collapsed"
                IsSettingsVisible="False"
                IsPaneToggleButtonVisible="False"
                PaneDisplayMode="Left"
                PaneOpening="OnNavPaneStateChanged"
                PaneClosing="OnNavPaneStateChanged"
                SelectionChanged="NavView_SelectionChanged">

            <NavigationView.Resources>
                <!-- Match Windows 11 Settings: ~22px colorful icons in a default-height row.
                     Default row pitch (~50px) lines up with Settings' sidebar.
                     Only the Left-pane theme key has an override; Top and Compact modes are not used (PaneDisplayMode is hardcoded above). -->
                <x:Double x:Key="NavigationViewItemOnLeftIconBoxHeight">22</x:Double>
                <!-- Tighten group separators so the section breaks don't feel as oversized
                     as the WinUI default once rows are bigger. -->
                <Thickness x:Key="NavigationViewItemSeparatorMargin">0,2,0,2</Thickness>

                <!-- Shared SvgImageSource resources for colorful sidebar icons.
                     Defined once so each NavigationViewItem references the same decoded SVG
                     (avoids per-instance parse cost; also makes the XAML→ImageSource path
                     explicit instead of relying on implicit URI→SvgImageSource conversion).
                     The dynamic agent-list code (HubWindow.xaml.cs) also looks up the
                     "Agents_Icon" key from this dictionary. -->
                <SvgImageSource x:Key="Chat_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Chat.svg"/>
                <SvgImageSource x:Key="Connection_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Connection.svg"/>
                <SvgImageSource x:Key="Sessions_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Sessions.svg"/>
                <SvgImageSource x:Key="Skills_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Skills.svg"/>
                <SvgImageSource x:Key="Channels_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Channels.svg"/>
                <SvgImageSource x:Key="Instances_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Instances.svg"/>
                <SvgImageSource x:Key="Advanced_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Advanced.svg"/>
                <SvgImageSource x:Key="AgentEvents_Icon" UriSource="ms-appx:///Assets/SidebarIcons/AgentEvents.svg"/>
                <SvgImageSource x:Key="Agents_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Agents.svg"/>
                <SvgImageSource x:Key="Bindings_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Bindings.svg"/>
                <SvgImageSource x:Key="Config_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Config.svg"/>
                <SvgImageSource x:Key="Usage_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Usage.svg"/>
                <SvgImageSource x:Key="Cron_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Cron.svg"/>
                <SvgImageSource x:Key="Voice_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Voice.svg"/>
                <SvgImageSource x:Key="Settings_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Settings.svg"/>
                <SvgImageSource x:Key="Permissions_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Permissions.svg"/>
                <SvgImageSource x:Key="Sandbox_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Sandbox.svg"/>
                <SvgImageSource x:Key="Activity_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Activity.svg"/>
                <SvgImageSource x:Key="Debug_Icon" UriSource="ms-appx:///Assets/SidebarIcons/Debug.svg"/>
            </NavigationView.Resources>

            <NavigationView.MenuItems>
                <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_82" x:Name="NavChat" Tag="chat" Content="Chat">
                    <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Chat_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItemSeparator/>

            <NavigationViewItemHeader x:Uid="HubWindow_NavigationViewItemHeader_87" x:Name="NavGatewayHeader" Content="Gateway"/>
            <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_88" Tag="connection" Content="Connection">
                <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Connection_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_91" x:Name="NavSessions" Tag="sessions" Content="Sessions">
                <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Sessions_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_97" x:Name="NavSkills" Tag="skills" Content="Skills">
                <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Skills_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_109" x:Name="NavChannels" Tag="channels" Content="Channels">
                <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Channels_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_112" x:Name="NavInstances" Tag="instances" Content="Instances">
                <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Instances_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_124" x:Name="NavCron" Tag="cron" Content="Cron">
                <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Cron_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
            </NavigationViewItem>
            <!-- Advanced — consolidates gateway-oriented pages -->
            <NavigationViewItem x:Uid="HubWindow_Advanced" x:Name="NavAdvanced" Content="Advanced" SelectsOnInvoked="False">
                <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Advanced_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
                <NavigationViewItem.MenuItems>
                    <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_94" Tag="agentevents" Content="Event Stream">
                        <NavigationViewItem.Icon><ImageIcon Source="{StaticResource AgentEvents_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem x:Uid="AgentsNavItem" x:Name="AgentsNavItem" Content="Agents" SelectsOnInvoked="False" IsExpanded="True">
                        <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Agents_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
                        <NavigationViewItem.MenuItems>
                            <NavigationViewItem x:Uid="DefaultAgentNavItem" x:Name="DefaultAgentNavItem" Content="main" Tag="agent:main">
                                <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Agents_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
                            </NavigationViewItem>
                        </NavigationViewItem.MenuItems>
                    </NavigationViewItem>
                    <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_115" Tag="bindings" Content="Bindings">
                        <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Bindings_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_118" x:Name="NavConfig" Tag="config" Content="Config">
                        <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Config_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
                    </NavigationViewItem>
                    <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_121" Tag="usage" Content="Usage">
                        <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Usage_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
                    </NavigationViewItem>
                </NavigationViewItem.MenuItems>
            </NavigationViewItem>
            <NavigationViewItemSeparator x:Name="NavGatewaySeparator"/>

            <NavigationViewItemHeader x:Uid="HubWindow_NavigationViewItemHeader_129" Content="This Computer"/>
            <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_Voice" Tag="voice" Content="Voice &amp; Audio">
                <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Voice_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_136" Tag="permissions" Content="Permissions">
                <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Permissions_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_Sandbox" Tag="sandbox" Content="Sandbox">
                <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Sandbox_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <NavigationView.FooterMenuItems>
            <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_145" x:Name="NavDiagnostics" Tag="debug" Content="Debug">
                <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Debug_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
            </NavigationViewItem>
            <NavigationViewItem x:Uid="HubWindow_NavigationViewItem_133" Tag="settings" Content="Settings">
                <NavigationViewItem.Icon><ImageIcon Source="{StaticResource Settings_Icon}" AutomationProperties.AccessibilityView="Raw"/></NavigationViewItem.Icon>
            </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" Navigated="OnContentFrameNavigated" NavigationFailed="OnContentFrameNavigationFailed">
                <Frame.ContentTransitions>
                    <TransitionCollection>
                        <NavigationThemeTransition/>
                    </TransitionCollection>
                </Frame.ContentTransitions>
            </Frame>
        </NavigationView>
        </Grid>

        <!-- Floating notification banner: lives in the content row (Row 2) and is
             declared after NavContentHost so it overlays the top of the content
             area instead of pushing the title bar / content down. The wrapper grid
             pins it to the right ~2/5 of the content width so it never reaches into
             the navigation pane. Multi-line messages grow the banner taller. -->
        <Grid Grid.Row="1" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <InfoBar x:Name="AppNotificationInfoBar"
                     Grid.Column="1"
                     Margin="12,8,12,0"
                     IsOpen="False"
                     Visibility="Collapsed"
                     IsClosable="True"
                     Closed="OnAppNotificationInfoBarClosed">
                <!-- Native InfoBar Title/Message (set in RenderAppNotification) plus
                     a standard ActionButton. The InfoBar itself wraps long messages
                     and moves the action button below the text responsively. -->
                <InfoBar.ActionButton>
                    <Button x:Name="AppNotificationActionButton"
                            Visibility="Collapsed"
                            Click="OnAppNotificationActionButtonClick"/>
                </InfoBar.ActionButton>
            </InfoBar>
        </Grid>

    </Grid>
</winex:WindowEx>
