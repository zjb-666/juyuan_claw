<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows10.0.22621.0</TargetFramework>
    <UseWinUI>true</UseWinUI>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <EnableMsixTooling>true</EnableMsixTooling>
    <ApplicationIcon>Assets\openclaw.ico</ApplicationIcon>
    <AssemblyName>JuyuanLingchuang</AssemblyName>
    <AssemblyTitle>聚元灵创</AssemblyTitle>
    <FileDescription>聚元灵创 Windows 智能助手</FileDescription>
    <Product>聚元灵创</Product>
    <RootNamespace>OpenClawTray</RootNamespace>
    <DefaultLanguage>en-US</DefaultLanguage>
    <Platforms>x64;ARM64</Platforms>
    <RuntimeIdentifiers>win-x64;win-arm64</RuntimeIdentifiers>
    <ProductApiBaseUrl Condition="'$(ProductApiBaseUrl)' == ''"></ProductApiBaseUrl>
    <GeneratedProductConfigPath>$(IntermediateOutputPath)product-config.json</GeneratedProductConfigPath>
  </PropertyGroup>

  <!-- Dev build identity: allows side-by-side installation with Release.
       Set DevBuild=true explicitly; release identity remains the default for every configuration. -->
  <PropertyGroup Condition="'$(DevBuild)' == 'true'">
    <DefineConstants>$(DefineConstants);DEV_BUILD</DefineConstants>
    <AppIdentityMarker>dev</AppIdentityMarker>
    <AssemblyTitle>聚元灵创 (Dev)</AssemblyTitle>
    <FileDescription>聚元灵创 Windows 智能助手 (Dev)</FileDescription>
    <Product>聚元灵创 (Dev)</Product>
  </PropertyGroup>
  <PropertyGroup Condition="'$(DevBuild)' != 'true'">
    <AppIdentityMarker>release</AppIdentityMarker>
  </PropertyGroup>

  <!-- Map $(Platform) to $(RuntimeIdentifier) so self-contained WinUI F5/debug produces a launchable EXE. -->
  <PropertyGroup Condition="'$(RuntimeIdentifier)' == '' and '$(Platform)' == 'x64'">
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
  </PropertyGroup>
  <PropertyGroup Condition="'$(RuntimeIdentifier)' == '' and '$(Platform)' == 'ARM64'">
    <RuntimeIdentifier>win-arm64</RuntimeIdentifier>
  </PropertyGroup>

  <!-- Unpackaged (default): traditional EXE distribution via Inno Setup -->
  <PropertyGroup Condition="'$(PackageMsix)' != 'true'">
    <WindowsPackageType>None</WindowsPackageType>
    <WindowsAppSDKSelfContained>true</WindowsAppSDKSelfContained>
    <ApplicationManifest>app.manifest</ApplicationManifest>
  </PropertyGroup>

  <!-- MSIX packaged: enables camera/mic consent prompts, package identity -->
  <PropertyGroup Condition="'$(PackageMsix)' == 'true'">
    <WindowsPackageType>MSIX</WindowsPackageType>
    <WindowsAppSDKSelfContained>true</WindowsAppSDKSelfContained>
    <AppxPackageSigningEnabled>false</AppxPackageSigningEnabled>
    <GenerateAppxPackageOnBuild>true</GenerateAppxPackageOnBuild>
    <AppxBundle>Never</AppxBundle>
    <UapAppxPackageBuildMode>SideloadOnly</UapAppxPackageBuildMode>
    <!-- MSIX package version is generated from $(Version) into the obj/ manifest by
         PrepareOpenClawAppxManifest below; this is the only place the package version is sourced. -->
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\OpenClaw.Shared\OpenClaw.Shared.csproj" />
    <ProjectReference Include="..\OpenClaw.Connection\OpenClaw.Connection.csproj" />
    <ProjectReference Include="..\OpenClaw.SetupEngine.UI\OpenClaw.SetupEngine.UI.csproj" />
    <ProjectReference Include="..\OpenClawTray.FunctionalUI\OpenClawTray.FunctionalUI.csproj" />
    <ProjectReference Include="..\OpenClaw.Chat\OpenClaw.Chat.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="OpenClaw.Tray.UITests" />
    <InternalsVisibleTo Include="OpenClaw.Tray.Tests" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.0" />
    <PackageReference Include="Microsoft.UI.Reactor" Version="0.1.0-preview.12" />
    <PackageReference Include="Microsoft.WindowsAppSDK" Version="$(MicrosoftWindowsAppSDKVersion)" />
    <PackageReference Include="Microsoft.Windows.SDK.BuildTools" Version="$(MicrosoftWindowsSdkBuildToolsVersion)" />
    <PackageReference Include="OpenTelemetry" Version="1.16.0" />
    <PackageReference Include="OpenTelemetry.Exporter.OpenTelemetryProtocol" Version="1.16.0" />
    <PackageReference Include="WinUIEx" Version="2.9.0" />
    <PackageReference Include="Microsoft.Toolkit.Uwp.Notifications" Version="7.1.3" />
    <PackageReference Include="CommunityToolkit.WinUI.Controls.SettingsControls" Version="8.3.260402-preview2" />
    <PackageReference Include="CommunityToolkit.WinUI.Controls.Sizers" Version="8.3.260402-preview2" />
    <PackageReference Include="System.Diagnostics.PerformanceCounter" Version="10.0.0" />
    <PackageReference Include="System.Drawing.Common" Version="10.0.9" />
    <PackageReference Include="System.Security.Cryptography.ProtectedData" Version="10.0.0" />
    <PackageReference Include="Updatum" Version="1.3.4" />
    <PackageReference Include="NAudio.Wasapi" Version="2.3.0" />
    <PackageReference Include="org.k2fsa.sherpa.onnx" Version="1.13.0" />
    <PackageReference Include="Zeroconf" Version="3.6.11" />
    <PackageReference Include="ZXing.Net" Version="0.16.10" />
  </ItemGroup>

  <ItemGroup>
    <Content Include="Assets\**\*" CopyToOutputDirectory="PreserveNewest" />
    <None Include="$(GeneratedProductConfigPath)"
          Link="product-config.json"
          CopyToOutputDirectory="PreserveNewest"
          Visible="false" />
    <None Include="..\OpenClaw.SetupEngine\default-config.json"
             Link="default-config.json"
             CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <PropertyGroup>
    <OpenClawRepoRoot>$([System.IO.Path]::GetFullPath('$(MSBuildThisFileDirectory)..\..\'))</OpenClawRepoRoot>
    <OpenClawSetupDefaultConfig>$(OpenClawRepoRoot)src\OpenClaw.SetupEngine\default-config.json</OpenClawSetupDefaultConfig>
    <!-- RID-derived arch for tools/mxc/<arch>/wxc-exec.exe. Falls back to x64
         when no RID is present (e.g. unpackaged debug builds). -->
    <MxcArch Condition="'$(MxcArch)' == '' And '$(RuntimeIdentifier)' == 'win-arm64'">arm64</MxcArch>
    <MxcArch Condition="'$(MxcArch)' == '' And '$(RuntimeIdentifier)' == 'win-x64'">x64</MxcArch>
    <MxcArch Condition="'$(MxcArch)' == '' And '$(PlatformTarget)' == 'ARM64'">arm64</MxcArch>
    <MxcArch Condition="'$(MxcArch)' == '' And '$(RuntimeIdentifier)' == ''">x64</MxcArch>
    <MxcSdkBinDir>$(OpenClawRepoRoot)node_modules\@microsoft\mxc-sdk\bin\$(MxcArch)\</MxcSdkBinDir>
    <MxcSdkExpectedVersion>0.7.0</MxcSdkExpectedVersion>
    <MxcSdkPackageJson>$(OpenClawRepoRoot)node_modules\@microsoft\mxc-sdk\package.json</MxcSdkPackageJson>
    <MxcSdkRestoreStamp>$(OpenClawRepoRoot)node_modules\.openclaw-mxc-sdk-$(MxcSdkExpectedVersion).stamp</MxcSdkRestoreStamp>
  </PropertyGroup>

  <Target Name="GenerateProductConfig" BeforeTargets="PrepareForBuild">
    <WriteLinesToFile
      File="$(GeneratedProductConfigPath)"
      Lines="{&quot;productApiBaseUrl&quot;:&quot;$(ProductApiBaseUrl)&quot;}"
      Overwrite="true"
      Encoding="UTF-8" />
  </Target>

  <!-- Generate the versioned/channel-specific manifest under obj/. Never rewrite the
       tracked source manifest: a dev package build followed by a release build must not
       inherit the dev identity, and parallel builds must not race on one source file. -->
  <UsingTask TaskName="GenerateOpenClawAppxManifest" TaskFactory="RoslynCodeTaskFactory" AssemblyFile="$(MSBuildToolsPath)\Microsoft.Build.Tasks.Core.dll">
    <ParameterGroup>
      <SourceManifestPath ParameterType="System.String" Required="true" />
      <OutputManifestPath ParameterType="System.String" Required="true" />
      <FourPartVersion ParameterType="System.String" Required="true" />
      <DevBuild ParameterType="System.String" />
      <IdentityName ParameterType="System.String" Required="true" />
      <DisplayName ParameterType="System.String" Required="true" />
      <ProtocolName ParameterType="System.String" Required="true" />
    </ParameterGroup>
    <Task>
      <Code Type="Fragment" Language="cs">
        <![CDATA[
        if (!System.Text.RegularExpressions.Regex.IsMatch(FourPartVersion, @"^\d+\.\d+\.\d+\.\d+$")) {
            Log.LogError("GenerateOpenClawAppxManifest: '" + FourPartVersion + "' is not a valid four-part MSIX version.");
            return false;
        }

        var text = System.IO.File.ReadAllText(SourceManifestPath);
        var versionRegex = new System.Text.RegularExpressions.Regex(
            "(<Identity\\b[^>]*?\\bVersion\\s*=\\s*[\"'])[^\"']+([\"'])",
            System.Text.RegularExpressions.RegexOptions.IgnoreCase);
        if (!versionRegex.IsMatch(text)) {
            Log.LogError("GenerateOpenClawAppxManifest: Identity/@Version missing from " + SourceManifestPath);
            return false;
        }
        text = versionRegex.Replace(text, "${1}" + FourPartVersion + "$2", 1);

        if (System.String.Equals(DevBuild, "true", System.StringComparison.OrdinalIgnoreCase)) {
            var nameRegex = new System.Text.RegularExpressions.Regex(
            "(<Identity\\b[^>]*?\\bName\\s*=\\s*[\"'])[^\"']+([\"'])",
            System.Text.RegularExpressions.RegexOptions.IgnoreCase);
            if (!nameRegex.IsMatch(text)) {
                Log.LogError("GenerateOpenClawAppxManifest: Identity/@Name missing from " + SourceManifestPath);
                return false;
            }
            text = nameRegex.Replace(text, "${1}" + IdentityName + "$2", 1);

            var propsDisplayRegex = new System.Text.RegularExpressions.Regex(
            "(<Properties>[\\s\\S]*?<DisplayName>)[^<]+(</DisplayName>)",
            System.Text.RegularExpressions.RegexOptions.IgnoreCase);
            if (!propsDisplayRegex.IsMatch(text)) {
                Log.LogError("GenerateOpenClawAppxManifest: Properties/DisplayName missing from " + SourceManifestPath);
                return false;
            }
            text = propsDisplayRegex.Replace(text, "${1}" + DisplayName + "$2", 1);

            var visualRegex = new System.Text.RegularExpressions.Regex(
            "(<uap:VisualElements\\b[\\s\\S]*?\\bDisplayName\\s*=\\s*[\"'])[^\"']+([\"'])",
            System.Text.RegularExpressions.RegexOptions.IgnoreCase);
            if (!visualRegex.IsMatch(text)) {
                Log.LogError("GenerateOpenClawAppxManifest: VisualElements/@DisplayName missing from " + SourceManifestPath);
                return false;
            }
            text = visualRegex.Replace(text, "${1}" + DisplayName + "$2", 1);

            var protocolRegex = new System.Text.RegularExpressions.Regex(
            "(<uap:Protocol\\b[^>]*?\\bName\\s*=\\s*[\"'])[^\"']+([\"'])",
            System.Text.RegularExpressions.RegexOptions.IgnoreCase);
            if (!protocolRegex.IsMatch(text)) {
                Log.LogError("GenerateOpenClawAppxManifest: Protocol/@Name missing from " + SourceManifestPath);
                return false;
            }
            text = protocolRegex.Replace(text, "${1}" + ProtocolName + "$2", 1);
        }

        System.IO.Directory.CreateDirectory(System.IO.Path.GetDirectoryName(OutputManifestPath));
        System.IO.File.WriteAllText(OutputManifestPath, text);
        Log.LogMessage(MessageImportance.High,
            "Generated " + OutputManifestPath + " (Version=" + FourPartVersion + ", DevBuild=" + DevBuild + ")");
        ]]>
      </Code>
    </Task>
  </UsingTask>

  <Target Name="PrepareOpenClawAppxManifest"
          BeforeTargets="_ValidatePresenceOfAppxManifestItems;_CalculateInputsForGenerateCurrentProjectAppxManifest"
          Condition="'$(WindowsPackageType)' == 'MSIX'">
    <Error Text="MSIX packaging requested but Version is empty. Pass /p:Version=X.Y[.Z]." Condition="'$(Version)' == ''" />
    <PropertyGroup>
      <_StrippedVersion>$([System.Text.RegularExpressions.Regex]::Replace('$(Version)', '[-+].*$', ''))</_StrippedVersion>
      <_VersionDotCount>$([System.Text.RegularExpressions.Regex]::Matches('$(_StrippedVersion)', '\.').Count)</_VersionDotCount>
      <_AppxManifestVersion Condition="'$(_VersionDotCount)' == '3'">$(_StrippedVersion)</_AppxManifestVersion>
      <_AppxManifestVersion Condition="'$(_VersionDotCount)' == '2'">$(_StrippedVersion).0</_AppxManifestVersion>
      <_AppxManifestVersion Condition="'$(_VersionDotCount)' == '1'">$(_StrippedVersion).0.0</_AppxManifestVersion>
      <_GeneratedAppxManifestPath>$(IntermediateOutputPath)openclaw.Package.appxmanifest</_GeneratedAppxManifestPath>
    </PropertyGroup>
    <Error Text="Cannot derive four-part MSIX version from $(Version). Use X.Y or X.Y.Z with an optional suffix." Condition="'$(_AppxManifestVersion)' == ''" />
    <GenerateOpenClawAppxManifest
      SourceManifestPath="$(MSBuildThisFileDirectory)Package.appxmanifest"
      OutputManifestPath="$(_GeneratedAppxManifestPath)"
      FourPartVersion="$(_AppxManifestVersion)"
      DevBuild="$(DevBuild)"
      IdentityName="Juyuan.Lingchuang.Dev"
      DisplayName="聚元灵创 (Dev)"
      ProtocolName="juyuanlingchuang-dev" />
    <ItemGroup>
      <AppxManifest Remove="@(AppxManifest)" />
      <AppxManifest Include="$(_GeneratedAppxManifestPath)">
        <SubType>Designer</SubType>
      </AppxManifest>
      <FileWrites Include="$(_GeneratedAppxManifestPath)" />
    </ItemGroup>
  </Target>

  <!-- Local installer tooling validates this marker before wrapping a published payload. -->
  <Target Name="WritePublishedAppIdentityMarker" AfterTargets="Publish">
    <WriteLinesToFile
      File="$(PublishDir)app-identity.txt"
      Lines="$(AppIdentityMarker)"
      Overwrite="true"
      Encoding="UTF-8" />
  </Target>

  <Target Name="WriteBuildAppIdentityMarker" AfterTargets="Build">
    <WriteLinesToFile
      File="$(TargetDir)app-identity.txt"
      Lines="$(AppIdentityMarker)"
      Overwrite="true"
      Encoding="UTF-8" />
  </Target>

  <!-- Fail fast on RIDs we have no wxc-exec binary for, so x86/non-Windows
       builds don't silently fall back to x64 and ship a broken sandbox.
       Suppress with -p:VerifyWxcExecShipped=false for SDK-less unit-test builds. -->
  <Target Name="ValidateMxcArchMapping" BeforeTargets="Build;Publish" Condition="'$(MxcArch)' == '' And '$(VerifyWxcExecShipped)' != 'false' And '$(SkipMxcNodeBridgeRestore)' != 'true'">
    <Error Text="Unsupported RuntimeIdentifier '$(RuntimeIdentifier)' for MXC sandbox. Only win-x64 and win-arm64 are supported. Override -p:MxcArch=x64 or -p:MxcArch=arm64 if you know what you are doing." />
  </Target>

  <Target Name="RestoreMxcNodeBridge"
          BeforeTargets="Build;Publish;CopyWxcExecToOutput;CopyWxcExecToPublish"
          Condition="'$(SkipMxcNodeBridgeRestore)' != 'true' And Exists('$(OpenClawRepoRoot)package-lock.json')"
          Inputs="$(OpenClawRepoRoot)package-lock.json"
          Outputs="$(MxcSdkRestoreStamp)">
    <Message Importance="high" Text="Restoring @microsoft/mxc-sdk to extract wxc-exec.exe ($(MxcArch))" />
    <Exec Command="npm ci --no-audit --no-fund" WorkingDirectory="$(OpenClawRepoRoot)" />
    <Touch Files="$(MxcSdkRestoreStamp)" AlwaysCreate="true" />
  </Target>

  <Target Name="ValidateMxcSdkVersion"
          BeforeTargets="CopyWxcExecToOutput;CopyWxcExecToPublish"
          DependsOnTargets="RestoreMxcNodeBridge"
          Condition="'$(SkipMxcNodeBridgeRestore)' != 'true' And Exists('$(OpenClawRepoRoot)package-lock.json')">
    <Error Condition="!Exists('$(MxcSdkPackageJson)')" Text="@microsoft/mxc-sdk package.json missing at $(MxcSdkPackageJson). Run `npm ci` at $(OpenClawRepoRoot)." />
    <Error Condition="Exists('$(MxcSdkPackageJson)') And !$([System.IO.File]::ReadAllText('$(MxcSdkPackageJson)').Contains('&quot;version&quot;: &quot;$(MxcSdkExpectedVersion)&quot;'))" Text="@microsoft/mxc-sdk version mismatch. Expected $(MxcSdkExpectedVersion) in $(MxcSdkPackageJson). Run `npm ci` at $(OpenClawRepoRoot)." />
  </Target>

  <!-- Copy ONLY wxc-exec.exe (and any sibling DLLs) for the active arch into
       $(TargetDir)tools/mxc/<arch>/. No node_modules ships. The C# probe in
       MxcAvailability.ResolveWxcExec looks here first. -->
  <Target Name="CopyWxcExecToOutput" AfterTargets="Build">
    <ItemGroup>
      <_WxcExecFiles Include="$(MxcSdkBinDir)wxc-exec.exe" />
      <_WxcExecFiles Include="$(MxcSdkBinDir)*.dll" />
    </ItemGroup>
    <Copy SourceFiles="@(_WxcExecFiles)" DestinationFiles="@(_WxcExecFiles->'$(TargetDir)tools\mxc\$(MxcArch)\%(Filename)%(Extension)')" SkipUnchangedFiles="true" Condition="'@(_WxcExecFiles)' != ''" />
  </Target>

  <!-- Standalone publish copy. Deliberately not chained to Build so
       dotnet publish with no-build still works. -->
  <Target Name="CopyWxcExecToPublish" AfterTargets="Publish" Condition="'$(PublishDir)' != ''">
    <ItemGroup>
      <_WxcExecPublishFiles Include="$(MxcSdkBinDir)wxc-exec.exe" />
      <_WxcExecPublishFiles Include="$(MxcSdkBinDir)*.dll" />
    </ItemGroup>
    <Copy SourceFiles="@(_WxcExecPublishFiles)" DestinationFiles="@(_WxcExecPublishFiles->'$(PublishDir)tools\mxc\$(MxcArch)\%(Filename)%(Extension)')" SkipUnchangedFiles="true" Condition="'@(_WxcExecPublishFiles)' != ''" />
  </Target>

  <!-- Fail the build if wxc-exec.exe didn't land in the expected output path.
       Catches RID/arch mismatches and silent npm ci skips. Gated on
       VerifyWxcExecShipped (default true) so unit-test-only builds without
       npm available can opt out. -->
  <Target Name="ValidateWxcExecShipped" AfterTargets="CopyWxcExecToOutput" Condition="'$(VerifyWxcExecShipped)' != 'false' And '$(SkipMxcNodeBridgeRestore)' != 'true'">
    <Error Condition="!Exists('$(TargetDir)tools\mxc\$(MxcArch)\wxc-exec.exe')" Text="wxc-exec.exe missing at $(TargetDir)tools\mxc\$(MxcArch)\. Check MxcArch=$(MxcArch), MxcSdkBinDir=$(MxcSdkBinDir). Run `npm ci` at $(OpenClawRepoRoot)." />
  </Target>

  <!-- Same validation, but after publish, catches publish-no-build runs that
       silently ship without wxc-exec. -->
  <Target Name="ValidateWxcExecPublished" AfterTargets="CopyWxcExecToPublish" Condition="'$(PublishDir)' != '' And '$(VerifyWxcExecShipped)' != 'false' And '$(SkipMxcNodeBridgeRestore)' != 'true'">
    <Error Condition="!Exists('$(PublishDir)tools\mxc\$(MxcArch)\wxc-exec.exe')" Text="wxc-exec.exe missing at $(PublishDir)tools\mxc\$(MxcArch)\. Publish-time copy did not run; check MxcArch=$(MxcArch), MxcSdkBinDir=$(MxcSdkBinDir)." />
  </Target>

  <!-- Copy WebView2Loader.dll to output root for unpackaged app (not needed for MSIX) -->
  <Target Name="CopyWebView2Loader" AfterTargets="Build" Condition="'$(PackageMsix)' != 'true'">
    <PropertyGroup>
      <!-- Detect ARM64 via RuntimeIdentifier, Platform, or processor architecture -->
      <IsArm64Build Condition="'$(Platform)' == 'ARM64' OR '$(RuntimeIdentifier)' == 'win-arm64' OR '$(PROCESSOR_ARCHITECTURE)' == 'ARM64'">true</IsArm64Build>
      <WebView2LoaderSource Condition="'$(IsArm64Build)' == 'true'">$(OutputPath)runtimes\win-arm64\native\WebView2Loader.dll</WebView2LoaderSource>
      <WebView2LoaderSource Condition="'$(WebView2LoaderSource)' == ''">$(OutputPath)runtimes\win-x64\native\WebView2Loader.dll</WebView2LoaderSource>
    </PropertyGroup>
    <Message Importance="high" Text="Copying WebView2Loader.dll from $(WebView2LoaderSource)" />
    <Copy SourceFiles="$(WebView2LoaderSource)" DestinationFolder="$(OutputPath)" SkipUnchangedFiles="true" Condition="Exists('$(WebView2LoaderSource)')" />
  </Target>

  <Target Name="CopySetupDefaultConfigToOutput" AfterTargets="Build">
    <Copy SourceFiles="$(OpenClawSetupDefaultConfig)" DestinationFiles="$(TargetDir)default-config.json" SkipUnchangedFiles="true" Condition="Exists('$(OpenClawSetupDefaultConfig)')" />
  </Target>

  <!--
    Tray's <Content Include="Assets\**\*"> matches Assets\Setup\*.png, so MSBuild
    propagates them as copy-to-output items through every transitive consumer
    (OpenClaw.Tray.UITests, etc.). WinUI's MakePri/Resources pipeline relocates
    those PNGs under bin\OpenClaw.SetupEngine.UI\Assets\Setup\ to match the
    ms-appx:///OpenClaw.SetupEngine.UI/... URIs and leaves bin\Assets\Setup\
    empty, which causes consumer builds to fail with MSB3030 "file not found".
    Re-copy the source PNGs to bin\Assets\Setup\ so the loose-file copy
    pipeline that consumers depend on is satisfied. Runtime loads still use the
    SetupEngine.UI-scoped copies.
  -->
  <Target Name="CopySetupAssetsLooseForConsumerCopy" AfterTargets="Build">
    <ItemGroup>
      <_SetupAssetSource Include="Assets\Setup\**\*" />
    </ItemGroup>
    <Copy SourceFiles="@(_SetupAssetSource)"
          DestinationFiles="@(_SetupAssetSource->'$(TargetDir)Assets\Setup\%(RecursiveDir)%(Filename)%(Extension)')"
          SkipUnchangedFiles="true" />
  </Target>

  <Target Name="CopySetupAssetsLooseForPublish" AfterTargets="Publish" Condition="'$(PublishDir)' != ''">
    <ItemGroup>
      <_SetupAssetPublishSource Include="Assets\Setup\**\*" />
    </ItemGroup>
    <Copy SourceFiles="@(_SetupAssetPublishSource)"
          DestinationFiles="@(_SetupAssetPublishSource->'$(PublishDir)Assets\Setup\%(RecursiveDir)%(Filename)%(Extension)')"
          SkipUnchangedFiles="true" />
  </Target>

  <Target Name="CopySetupDefaultConfigToPublish" AfterTargets="Publish" Condition="'$(PublishDir)' != ''">
    <Copy SourceFiles="$(OpenClawSetupDefaultConfig)" DestinationFiles="$(PublishDir)default-config.json" SkipUnchangedFiles="true" Condition="Exists('$(OpenClawSetupDefaultConfig)')" />
  </Target>

</Project>
