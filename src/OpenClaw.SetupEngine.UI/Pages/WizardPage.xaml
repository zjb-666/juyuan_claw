<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClaw.SetupEngine.UI.Pages.WizardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Disabled">

    <Grid RowDefinitions="Auto,*,Auto" Padding="48,28,48,24">
        <StackPanel Grid.Row="0" Spacing="6" HorizontalAlignment="Center">
            <Image Source="ms-appx:///OpenClaw.SetupEngine.UI/Assets/Setup/OpenClawMascot.png"
                   Width="44" Height="44" HorizontalAlignment="Center"
                   AutomationProperties.AccessibilityView="Raw" />
            <TextBlock Text="聚元灵创引导"
                       Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center" />
            <TextBlock x:Name="StatusText" Text="Connecting to gateway..."
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center" />
        </StackPanel>

        <ScrollViewer x:Name="MainScroller" Grid.Row="1" Margin="0,24,0,20" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="12">
            <StackPanel x:Name="TranscriptPanel" Spacing="6" />
            <Border x:Name="StepCard"
                    CornerRadius="8"
                    Padding="24"
                    MinHeight="180"
                    BorderThickness="1"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="16">
                    <TextBlock x:Name="TitleText" Style="{StaticResource SubtitleTextBlockStyle}" TextWrapping="Wrap" />
                    <RichTextBlock x:Name="MessageBlock" Visibility="Collapsed"
                                   IsTextSelectionEnabled="True" TextWrapping="Wrap"
                                   FontSize="14" Opacity="0.82" />
                    <StackPanel x:Name="MessageCodeRows" Spacing="8" />
                    <Border x:Name="ConsoleBanner"
                            Visibility="Collapsed"
                            CornerRadius="6"
                            Padding="12,10"
                            Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                            BorderThickness="1"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="6">
                            <TextBlock Text="Live gateway output"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <RichTextBlock x:Name="ConsoleBannerBlock"
                                           IsTextSelectionEnabled="True" TextWrapping="Wrap"
                                           FontSize="13" Opacity="0.92"
                                           FontFamily="Consolas" />
                            <StackPanel x:Name="ConsoleBannerLines" Spacing="6" />
                        </StackPanel>
                    </Border>
                    <ListView x:Name="SelectOptions" Visibility="Collapsed" SelectionMode="Single" />
                    <StackPanel x:Name="MultiOptions" Visibility="Collapsed" Spacing="8" />
                    <TextBox x:Name="TextInput" Visibility="Collapsed" PlaceholderText="Enter value" />
                    <PasswordBox x:Name="SecretInput" Visibility="Collapsed" PlaceholderText="Enter value" />
                    <TextBlock x:Name="ErrorText"
                               Visibility="Collapsed"
                               Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                               TextWrapping="Wrap"
                               IsTextSelectionEnabled="True" />
                    <StackPanel x:Name="GatewayRecovery"
                                Visibility="Collapsed"
                                Orientation="Horizontal"
                                Spacing="12">
                        <Button x:Name="OpenGatewayTerminalButton"
                                Content="Open terminal"
                                MinWidth="100"
                                Click="OpenGatewayTerminal_Click" />
                        <Button x:Name="RestartGatewayButton"
                                Content="Restart gateway"
                                MinWidth="100"
                                Click="RestartGateway_Click" />
                    </StackPanel>
                    <ProgressRing x:Name="BusyRing" Width="32" Height="32" IsActive="True" HorizontalAlignment="Center" Foreground="{ThemeResource SetupIndicatorAccentBrush}" />
                </StackPanel>
            </Border>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" ColumnDefinitions="*,Auto">
            <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="8"
                        HorizontalAlignment="Center" VerticalAlignment="Center"
                        IsTabStop="False" AutomationProperties.Name="Step 5 of 6">
                <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
                <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
                <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
                <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
                <Border Width="20" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupIndicatorAccentBrush}" />
                <Border Width="8" Height="8" CornerRadius="4" VerticalAlignment="Center" Background="{ThemeResource SetupInactiveDotBrush}" />
            </StackPanel>
            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left" Spacing="8">
                <Button x:Name="WizardBackButton" Content="Back"
                        MinWidth="100"
                        Click="WizardBack_Click" />
                <DropDownButton x:Name="MoreOptionsButton"
                                Content="More options"
                                MinWidth="100"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                Visibility="Collapsed"
                                AutomationProperties.Name="More onboarding options">
                    <DropDownButton.Flyout>
                        <MenuFlyout Placement="Top">
                            <MenuFlyoutItem Text="Restart onboard" Click="StartOver_Click">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE72C;" IsTextScaleFactorEnabled="False" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem Text="Skip &amp; exit" Click="SkipWizard_Click">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE72A;" IsTextScaleFactorEnabled="False" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                </DropDownButton>
            </StackPanel>
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Spacing="12">
                <Button x:Name="SecondaryButton" Content="Skip"
                        MinWidth="100"
                        Click="Secondary_Click" IsEnabled="False" />
                <Button x:Name="PrimaryButton" Content="Continue"
                        MinWidth="100"
                        Style="{StaticResource AccentButtonStyle}"
                        Click="Primary_Click" IsEnabled="False" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
